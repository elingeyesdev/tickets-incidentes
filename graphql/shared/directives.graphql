# ================================================================================
# DIRECTIVAS COMPARTIDAS - Sistema Helpdesk V8.0
# ================================================================================

# NOTA: Las directivas personalizadas (@company, @rateLimit, @audit) están
# definidas en sus clases PHP correspondientes en app/Shared/GraphQL/Directives/
# y se registran automáticamente en config/lighthouse.php
#
# Las directivas built-in de Lighthouse (@auth, @can, @cache, @guard, etc.)
# están disponibles automáticamente sin necesidad de definirlas aquí