╔════════════════════════════════════════════════════════════════════════════════╗
║                         ESTRUCTURA VISUAL DE TESTS                             ║
╚════════════════════════════════════════════════════════════════════════════════╝

/tests/
│
├─── 📁 Feature/              ← BACKEND (Laravel/PHP) ✅ YA EXISTEN
│    ├── Authentication/      • LoginMutationTest.php
│    │   ├── LoginMutationTest.php
│    │   ├── RegisterMutationTest.php
│    │   ├── RefreshTokenControllerTest.php
│    │   ├── LogoutMutationTest.php
│    │   └── ... 4 tests más
│    │
│    ├── CompanyManagement/   • CRUD de empresas
│    ├── GraphQL/             • Queries y mutations
│    └── UserManagement/      • CRUD de usuarios
│
├─── 📁 Unit/                 ← BACKEND Unit Tests
│    └── ExampleTest.php
│
├─── 📁 GraphQL/              ← BACKEND GraphQL Tests
│    └── BasicQueriesTest.php
│
├─── 📁 auth/                 ← FRONTEND (React/TS) 🆕
│    ├── TokenManager.test.ts        • En progreso
│    └── TokenRefreshService.test.ts • En progreso
│
├─── 📁 integration/          ← FRONTEND Integration Tests 🆕
│    └── auth-flow.test.ts    • ✅ Funcionando
│
├─── 📁 mocks/                ← FRONTEND Mocks 🆕
│    └── handlers.ts          • MSW endpoints
│
├─── setup.ts                 ← FRONTEND Setup 🆕
├─── TestCase.php             ← BACKEND Setup
└─── (otros archivos)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

RESUMEN RÁPIDO:

┌────────────────────────┬──────────────────────────┐
│ BACKEND (Laravel/PHP)  │ FRONTEND (React/TS)      │
├────────────────────────┼──────────────────────────┤
│ tests/Feature/         │ tests/auth/              │
│ tests/Unit/            │ tests/integration/       │
│ tests/GraphQL/         │ tests/components/        │
│ php artisan test       │ npm run test             │
└────────────────────────┴──────────────────────────┘

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

¿DÓNDE VAS A AGREGAR TU TEST?

┌─ ¿Es código PHP/Laravel/GraphQL backend?
│
├─→ SÍ: tests/Feature/ o tests/Unit/
│   Ejecutar: php artisan test tests/Feature/...
│
└─ ¿Es código React/TypeScript/Frontend?
   
   ├─→ Tests de SERVICIOS (TokenManager, etc)?
   │  tests/auth/
   │  Ejecutar: npm run test -- tests/auth
   │
   ├─→ Tests de FLUJO COMPLETO (login→onboarding)?
   │  tests/integration/
   │  Ejecutar: npm run test -- tests/integration
   │
   └─→ Tests de COMPONENTES (AuthGuard, Login)?
      tests/components/ (crear carpeta)
      Ejecutar: npm run test -- tests/components

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

EJEMPLOS:

BACKEND - Nuevo test para mutation UpdateProfile:
  Archivo: tests/Feature/UserManagement/UpdateProfileMutationTest.php
  Ejecutar: php artisan test tests/Feature/UserManagement/UpdateProfileMutationTest.php

FRONTEND - Nuevo test para service PersistenceService:
  Archivo: tests/auth/PersistenceService.test.ts
  Ejecutar: npm run test -- tests/auth/PersistenceService.test.ts

FRONTEND - Nuevo test para componente AuthGuard:
  Archivo: tests/components/AuthGuard.test.tsx
  Ejecutar: npm run test -- tests/components/AuthGuard.test.tsx

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

CARPETAS PRINCIPALES:

✅ tests/Feature/                = Pruebas de FEATURE completas (Backend)
✅ tests/Unit/                   = Pruebas UNITARIAS (Backend)
✅ tests/GraphQL/                = Pruebas GraphQL (Backend)

🆕 tests/auth/                   = Pruebas de SERVICIOS auth (Frontend)
🆕 tests/integration/            = Pruebas de FLUJOS (Frontend)
🆕 tests/components/ (crear)     = Pruebas de COMPONENTES (Frontend)
🆕 tests/e2e/ (crear)            = Pruebas END-TO-END (Frontend)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
